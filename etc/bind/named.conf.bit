// zone loading all namecoin .bit domains
zone "bit" {
	type master;
	file "/etc/bind/dotbit/db.namecoin.bit";

	allow-query { any; };
	allow-transfer { none; };

	forwarders { };

	check-names ignore;
};

